jwt:
  secret: ${JWT_SECRET:chave-de-desenvolvimento-apenas-nao-usar-em-producao}

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.healthcare.api: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.security: DEBUG

springdoc:
  swagger-ui:
    enabled: true